node_modules/
dist/
*.log

# Datasets live on E:\ per CLAUDE.md; repo only holds pointer/config
/data/

# Python
__pycache__/
*.pyc
.venv/

# Training artifacts — regenerable, large binary outputs
/training/checkpoints/
/training/runs/
/training/*.txt

# ONNX exports and diff fixtures — regenerable from training/src via export/
/export/*.onnx
/export/*.onnx.data
/export/*.bin

# Inference harness's copies of generated/vendor binaries — not source
/inference/public/models/*.onnx
/inference/public/models/*.onnx.data
/inference/public/*.bin
/inference/public/ort/

# OS
Thumbs.db
.DS_Store
